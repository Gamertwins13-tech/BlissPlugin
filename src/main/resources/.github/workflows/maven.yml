name: Java CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: mvn clean package
      - name: Upload
        uses: actions/upload-artifact@v3
        with:
          name: BlissPlugin
          path: target/*.jar
